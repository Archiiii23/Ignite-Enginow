DATABASE_URL=postgresql://user:password@localhost:5432/enginow
# Optional: scrypt hash used to provision the bundled seed personas on first startup.
SEED_PASSWORD_HASH=

# Optional: Real Google OAuth 2.0 Client ID from Google Cloud Console.
# If omitted or left empty, the application uses seamless instant Google account selection.
VITE_GOOGLE_CLIENT_ID=